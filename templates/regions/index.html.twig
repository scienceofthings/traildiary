{% extends 'base.html.twig' %}

{% block title %}
    {% trans %}regions{% endtrans %}
{% endblock %}



{% block container %}

            <h1>{% trans %}regions{% endtrans %}</h1>

            <a class="btn btn-primary" href="{{ path('trail_new') }} " role="button">
                {% trans %}create_trail{% endtrans %}
            </a>

            <a class="btn btn-primary" href="{{ path('region_new') }}" role="button">
                {% trans %}create_region{% endtrans %}
            </a>

{% endblock %}
