{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tour list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>altText</th>
                <th>file</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tourimage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.altText }}</td>
                <td><a href="{{ vich_uploader_asset(entity, 'gpx_file') }}">GPX</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tourimage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
                
                <a href="{{ path('map_homepage') }}">
            Karte
</a>
    {% endblock %}
