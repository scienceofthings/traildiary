{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tour list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Gpxfile</th>
                <th>Markerlat</th>
                <th>Markerlon</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tour_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.gpxfile }}</td>
                <td>{{ entity.markerlat }}</td>
                <td>{{ entity.markerlon }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tour_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tour_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tour_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
                
                <a href="{{ path('map_homepage') }}">
            Karte
</a>
    {% endblock %}
